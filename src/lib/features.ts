/**
 * 켜고 끄는 기능 스위치.
 *
 * 끄는 기능의 코드는 지우지 않는다. 지우면 다시 켤 때 콘솔 설정 · 토큰 정책을 처음부터 다시 정해야 한다.
 */

/**
 * 로그인(카카오 · 구글) 사용 여부.
 *
 * 2026-09-17 부터 꺼 두었다 (원티드 의견). 로그인 없이 기기당 하루 3회를 쓴다
 * (`docs/product.md` "문서 입력 · 로그인"). 꺼 두면 로그인 유도 시트 · 설정의 계정 구역이 보이지 않고,
 * 앱을 켤 때 로그인을 되살리지 않으며, 백엔드가 토큰 대신 기기 번호로 이용 횟수를 센다.
 *
 * 다시 켤 때는 순서대로 확인한다:
 * 1. 이 값을 true 로 바꾼다
 * 2. 구글 Auth Platform 에서 "앱 게시" 를 누른다. 게시 상태가 "테스트" 면 테스트 사용자로 등록한
 *    계정만 로그인된다 (`docs/api-contract.md` "콘솔 설정")
 * 3. Vercel 환경 변수에 VITE_KAKAO_REST_API_KEY · VITE_GOOGLE_CLIENT_ID 가 있는지 본다
 * 4. 백엔드에 계정 기준 하루 한도를 확인한다 (기기 기준과 별개 카운터)
 *
 * 타입을 boolean 으로 넓혀 둔다. 리터럴 타입(false)이면 TypeScript 가 로그인 쪽 갈래를 닿지 않는
 * 코드로 보고 타입 검사에서 빼, 꺼 둔 동안 그 코드가 조용히 낡는다.
 */
export const LOGIN_ENABLED: boolean = false
